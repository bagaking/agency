## 1. Specification
- [x] 1.1 Update `agency-editor` spec for typed child session creation actions.

## 2. Implementation
- [x] 2.1 Add `Sub Terminal` and `Fork` actions to the Agent Cells session context menu.
- [x] 2.2 Create child sessions with persisted `parentSessionId` and `nodeKind` metadata.
- [x] 2.3 Keep selection and existing session menu behaviors intact.

## 3. Validation
- [x] 3.1 Update README/manual verification notes for child session creation.
- [x] 3.2 Run unit tests and both typecheck commands.
